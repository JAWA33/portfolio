import React from "react";

const ProjectSubTitle = ({ title }) => {
  return (
    <div className="elementProject--large projectSubTitle">
      <h3 className="font__title">{title}</h3>
    </div>
  );
};

export default ProjectSubTitle;
